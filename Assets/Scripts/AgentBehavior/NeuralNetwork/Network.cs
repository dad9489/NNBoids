using UnityEngine;

namespace AgentBehavior.NeuralNetwork {
    public class Network {
        private Genome _genome;

        public Network(int numInputs, int numOutputs) {
//            _genome = new Genome(numInputs, numOutputs);
        }
        
    }
}